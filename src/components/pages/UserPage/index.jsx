import styled from "styled-components";

export default function UserPage() {
    return (
        <UserContainer>
            
        </UserContainer>
    )
}

const UserContainer = styled.main`
    width: 100%;
    min-height: 100vh;
`